'use client';
import Link from 'next/link'; import {usePathname} from 'next/navigation'; import {LayoutDashboard,FolderKanban,CheckSquare2,Clock3,Code2,BarChart3,Files,Settings,LogOut,Plus} from 'lucide-react'; import {createClient} from '@/lib/supabase/client';
const links=[['/dashboard','Overview',LayoutDashboard],['/projects','Projects',FolderKanban],['/tasks','Tasks',CheckSquare2],['/timeline','Timeline',Clock3],['/technologies','Technologies',Code2],['/analytics','Analytics',BarChart3],['/files','Files',Files],['/settings','Settings',Settings]] as const;
export default function AppShell({children,title,subtitle}:{children:React.ReactNode,title:string,subtitle?:string}){const path=usePathname();async function signout(){await createClient().auth.signOut();location.href='/login'}return <div className="app"><aside><div className="brand"><span className="brandmark">P</span><div><b>ProjectOS</b><small>Personal dashboard</small></div></div><nav>{links.map(([href,label,Icon])=><Link key={href} href={href} className={path.startsWith(href)?'active':''}><Icon size={18}/>{label}</Link>)}</nav><button className="signout" onClick={signout}><LogOut size={18}/>Sign out</button></aside><main className="workspace"><header><div><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}</div><Link className="primary compact" href="/projects/new"><Plus size={17}/>New project</Link></header>{children}</main></div>}
