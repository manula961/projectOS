'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {createClient} from '@/lib/supabase/client'; import type {Project} from '@/types/project';
export default function ProjectForm({project}:{project?:Project}){const r=useRouter();const s=createClient();const [saving,setSaving]=useState(false);const [err,setErr]=useState('');const [form,setForm]=useState({name:project?.name||'',slug:project?.slug||'',short_description:project?.short_description||'',full_description:project?.full_description||'',cover_url:project?.cover_url||'',category:project?.category||'',status:project?.status||'idea',priority:project?.priority||'medium',progress:project?.progress||0,github_url:project?.github_url||'',live_url:project?.live_url||'',docs_url:project?.docs_url||'',api_url:project?.api_url||'',hosting_provider:project?.hosting_provider||'',visibility:project?.visibility||'private',featured:project?.featured||false,start_date:project?.start_date||'',target_date:project?.target_date||''});
function set(k:string,v:any){setForm(x=>({...x,[k]:v}))} async function save(e:React.FormEvent){e.preventDefault();setSaving(true);setErr('');const {data:{user}}=await s.auth.getUser();if(!user){setErr('Session expired');setSaving(false);return}const payload={...form,slug:form.slug||form.name.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,''),start_date:form.start_date||null,target_date:form.target_date||null,user_id:user.id};const q=project?await s.from('projects').update(payload).eq('id',project.id):await s.from('projects').insert(payload).select('id').single();if(q.error){setErr(q.error.message);setSaving(false);return}const id=project?.id||(q.data as any).id;await s.from('activity_logs').insert({user_id:user.id,project_id:id,action:project?'project.updated':'project.created'});r.push(`/projects/${id}`);r.refresh()}
return <form className="form-grid" onSubmit={save}><section className="panel span2"><h2>Project identity</h2><div className="fields two"><label>Name<input required value={form.name} onChange={e=>set('name',e.target.value)}/></label><label>Slug<input value={form.slug} onChange={e=>set('slug',e.target.value)} placeholder="auto-generated"/></label><label className="span2">Short description<input value={form.short_description} onChange={e=>set('short_description',e.target.value)}/></label><label className="span2">Full description<textarea rows={6} value={form.full_description} onChange={e=>set('full_description',e.target.value)}/></label><label className="span2">Cover image URL<input type="url" value={form.cover_url} onChange={e=>set('cover_url',e.target.value)}/></label></div></section><section className="panel"><h2>State</h2><div className="fields"><label>Status<select value={form.status} onChange={e=>set('status',e.target.value)}>{['idea','planning','development','testing','deployed','completed','paused','archived'].map(x=><option key={x}>{x}</option>)}</select></label><label>Priority<select value={form.priority} onChange={e=>set('priority',e.target.value)}><option>low</option><option>medium</option><option>high</option></select></label><label>Progress — {form.progress}%<input type="range" min="0" max="100" value={form.progress} onChange={e=>set('progress',Number(e.target.value))}/></label><label>Visibility<select value={form.visibility} onChange={e=>set('visibility',e.target.value)}><option>private</option><option>unlisted</option><option>public</option></select></label><label className="check"><input type="checkbox" checked={form.featured} onChange={e=>set('featured',e.target.checked)}/> Featured project</label></div></section><section className="panel"><h2>Planning</h2><div className="fields"><label>Category<input value={form.category} onChange={e=>set('category',e.target.value)}/></label><label>Start date<input type="date" value={form.start_date} onChange={e=>set('start_date',e.target.value)}/></label><label>Target date<input type="date" value={form.target_date} onChange={e=>set('target_date',e.target.value)}/></label><label>Hosting provider<input value={form.hosting_provider} onChange={e=>set('hosting_provider',e.target.value)}/></label></div></section><section className="panel span2"><h2>Links</h2><div className="fields two"><label>GitHub URL<input type="url" value={form.github_url} onChange={e=>set('github_url',e.target.value)}/></label><label>Live URL<input type="url" value={form.live_url} onChange={e=>set('live_url',e.target.value)}/></label><label>Documentation URL<input type="url" value={form.docs_url} onChange={e=>set('docs_url',e.target.value)}/></label><label>API URL<input type="url" value={form.api_url} onChange={e=>set('api_url',e.target.value)}/></label></div></section><div className="form-actions span2">{err&&<span className="error">{err}</span>}<button className="primary" disabled={saving}>{saving?'Saving…':project?'Save changes':'Create project'}</button></div></form>}
