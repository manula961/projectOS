'use client';import {useEffect,useMemo,useState} from 'react';import {useRouter} from 'next/navigation';import {Search} from 'lucide-react';const actions=[['Overview','/dashboard'],['Projects','/projects'],['New project','/projects/new'],['GitHub Sync Center','/github'],['Timeline','/timeline'],['Project Health','/health'],['Technologies','/technologies'],['Files','/files'],['Settings','/settings']] as const;export default function CommandPalette(){const [open,setOpen]=useState(false),[q,setQ]=useState('');const r=useRouter();useEffect(()=>{const h=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(v=>!v)}if(e.key==='Escape')setOpen(false)};addEventListener('keydown',h);return()=>removeEventListener('keydown',h)},[]);const list=useMemo(()=>actions.filter(a=>a[0].toLowerCase().includes(q.toLowerCase())),[q]);if(!open)return <button className="command-trigger" onClick={()=>setOpen(true)}><Search size={15}/>Search <kbd>Ctrl K</kbd></button>;return <div className="modal-backdrop" onMouseDown={()=>setOpen(false)}><div className="command-card" onMouseDown={e=>e.stopPropagation()}><label className="command-search"><Search/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Go to anything…"/></label><div>{list.map(([name,path])=><button key={path} onClick={()=>{setOpen(false);r.push(path)}}>{name}<span>{path}</span></button>)}</div></div></div>}