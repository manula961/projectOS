'use client';
import {useMemo,useState} from 'react'; import ProjectCard from './ProjectCard'; import type {Project} from '@/types/project'; import {Search} from 'lucide-react';
export default function DashboardClient({projects}:{projects:Project[]}){const [q,setQ]=useState('');const [status,setStatus]=useState('all');const filtered=useMemo(()=>projects.filter(p=>(status==='all'||p.status===status)&&(`${p.name} ${p.category||''} ${p.short_description||''}`.toLowerCase().includes(q.toLowerCase()))),[projects,q,status]);return <><div className="toolbar"><label className="search"><Search size={17}/><input placeholder="Search projects…" value={q} onChange={e=>setQ(e.target.value)}/></label><select value={status} onChange={e=>setStatus(e.target.value)}><option value="all">All statuses</option>{['idea','planning','development','testing','deployed','completed','paused','archived'].map(x=><option key={x}>{x}</option>)}</select></div><div className="project-grid">{filtered.map(p=><ProjectCard key={p.id} p={p}/>)}{!filtered.length&&<div className="empty">No projects match this view.</div>}</div></>}
