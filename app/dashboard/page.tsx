import AppShell from '@/components/AppShell';import DashboardClient from '@/components/DashboardClient';import {createClient} from '@/lib/supabase/server';
export default async function Dashboard(){const s=await createClient();const {data:projects=[]}=await s.from('projects').select('*').neq('status','archived').order('featured',{ascending:false}).order('updated_at',{ascending:false});const p=projects||[];const active=p.filter(x=>['planning','development','testing'].includes(x.status)).length;const done=p.filter(x=>x.status==='completed'||x.status==='deployed').length;const avg=p.length?Math.round(p.reduce((a,x)=>a+x.progress,0)/p.length):0;const {count:tasks}=await s.from('project_tasks').select('*',{count:'exact',head:true}).neq('status','done');return <AppShell title="Overview" subtitle="Everything you are building, in one place."><div className="stats"><div><span>Projects</span><strong>{p.length}</strong><small>Total stored</small></div><div><span>Active</span><strong>{active}</strong><small>In progress</small></div><div><span>Delivered</span><strong>{done}</strong><small>Completed/deployed</small></div><div><span>Open tasks</span><strong>{tasks||0}</strong><small>Need attention</small></div><div><span>Avg. progress</span><strong>{avg}%</strong><small>Across projects</small></div></div><div className="section-heading"><div><h2>Project previews</h2><p>Pinned and recently updated projects</p></div></div><DashboardClient projects={p}/></AppShell>}
