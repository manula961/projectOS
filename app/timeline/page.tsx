import AppShell from '@/components/AppShell';import {createClient} from '@/lib/supabase/server';export default async function Timeline(){const s=await createClient();const {data=[]}=await s.from('project_updates').select('*,projects(name)').order('created_at',{ascending:false}).limit(100);return <AppShell title="Timeline" subtitle="Release and development history"><div className="timeline">{(data||[]).map(x=><article key={x.id}><time>{new Date(x.created_at).toLocaleDateString()}</time><div><span>{(x.projects as any)?.name}</span><h2>{x.title}</h2><p>{x.body}</p>{x.version&&<b>v{x.version.replace(/^v/,'')}</b>}</div></article>)}{!data?.length&&<div className="empty">No updates yet. Add updates inside a project.</div>}</div></AppShell>}
