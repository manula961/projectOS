import AppShell from '@/components/AppShell';import SettingsForm from '@/components/SettingsForm';import {createClient} from '@/lib/supabase/server';export default async function Settings(){const s=await createClient();const {data:{user}}=await s.auth.getUser();const {data:profile}=user?await s.from('profiles').select('*').eq('id',user.id).maybeSingle():{data:null};return <AppShell title="Settings" subtitle="Profile, integrations and security"><SettingsForm profile={profile} email={user?.email||''}/></AppShell>}
