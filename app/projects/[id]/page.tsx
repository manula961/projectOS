import {notFound} from 'next/navigation';import AppShell from '@/components/AppShell';import ProjectDetailClient from '@/components/ProjectDetailClient';import {createClient} from '@/lib/supabase/server';
export default async function ProjectPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createClient();const [p,t,m,u,n,l,f,tech,pt]=await Promise.all([s.from('projects').select('*').eq('id',id).single(),s.from('project_tasks').select('*').eq('project_id',id).order('created_at'),s.from('project_milestones').select('*').eq('project_id',id).order('due_date'),s.from('project_updates').select('*').eq('project_id',id).order('created_at',{ascending:false}),s.from('project_notes').select('*').eq('project_id',id).order('updated_at',{ascending:false}),s.from('project_links').select('*').eq('project_id',id),s.from('project_files').select('*').eq('project_id',id).order('created_at',{ascending:false}),s.from('technologies').select('*').order('name'),s.from('project_technologies').select('technology_id').eq('project_id',id)]);if(!p.data)notFound();return <AppShell title={p.data.name} subtitle="Project workspace"><ProjectDetailClient project={p.data} tasks={t.data||[]} milestones={m.data||[]} updates={u.data||[]} notes={n.data||[]} links={l.data||[]} files={f.data||[]} technologies={tech.data||[]} assignedTech={(pt.data||[]).map(x=>x.technology_id)}/></AppShell>}
