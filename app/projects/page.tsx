import AppShell from '@/components/AppShell';import DashboardClient from '@/components/DashboardClient';import {createClient} from '@/lib/supabase/server';export default async function Projects(){const s=await createClient();const {data=[]}=await s.from('projects').select('*').order('updated_at',{ascending:false});return <AppShell title="Projects" subtitle="Your complete project database"><DashboardClient projects={data||[]}/></AppShell>}
