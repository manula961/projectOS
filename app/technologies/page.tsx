import AppShell from '@/components/AppShell';import TechnologyManager from '@/components/TechnologyManager';import {createClient} from '@/lib/supabase/server';export default async function Technologies(){const s=await createClient();const {data=[]}=await s.from('technologies').select('*').order('name');return <AppShell title="Technologies" subtitle="Reusable technology catalog for your projects"><TechnologyManager items={data||[]}/></AppShell>}
